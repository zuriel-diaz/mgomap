package mgomap

const (
	MapperError 			= "Mapper is null"
	SessionError 			= "Mgo session is null"
	DocumentFormatError 	= "The document not have a correct format see: [DocumentBase]"
	DocumentExisting		= "The docuemnt exists. A new document is needed"
)